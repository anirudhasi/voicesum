fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9
pydantic>=2.10.0
pydantic-settings>=2.3.1
# SQLite async driver
aiosqlite>=0.19.0
SQLAlchemy>=2.0.0
python-jose[cryptography]==3.3.0
# passlib removed: unmaintained since 2020 and broken against current bcrypt.
# routers/auth.py uses bcrypt directly.
whisperx>=3.8.4
pandas
# whisperx pins numpy<2.1; librosa 1.x wants >=2.1. 2.5.x satisfies both.
numpy>=2.0.2,<3
scipy
soundfile==0.12.1
librosa
aiofiles==23.2.1
python-dotenv==1.0.1
pydantic[email]==2.13.4
# Optional: install pyannote only when HF_TOKEN is set
# 4.x is required: the community-1 pipeline needs it, and 3.x fails against
# torchaudio >=2.9 with "module 'torchaudio' has no attribute 'AudioMetaData'".
pyannote.audio>=4.0
# >=4.0.1 is required by chromadb; <6 keeps a known API surface.
bcrypt>=4.0.1,<6
# Speaker embedding - SpeechBrain ECAPA-TDNN
speechbrain>=1.0.0
# PDF generation
reportlab>=4.0.0
# PDF attachment support (DOCX parsing + PDF merging)
python-docx>=1.1.0
PyMuPDF>=1.24.0
# ── Local Qwen3 4B Instruct (4-bit quantized) ─────────────────
transformers>=4.45.0
bitsandbytes>=0.43.0
accelerate>=0.26.0
sentencepiece
protobuf
# -- Agenda/Context file extraction & Video OCR
python-pptx>=0.6.21
rapidocr-onnxruntime>=1.4.0
pypdf>=3.0.0
# ── RAG / Vector Store ─────────────────────────────────────────────────────
# ChromaDB for persistent vector indexing with metadata support
chromadb>=0.5.0
# ── Video OCR pipeline ─────────────────────────────────────────────────────
# Pillow for image handling
Pillow>=10.0.0

