# Development and evaluation tooling. Not needed to run the application.
# Install with the same torch constraint as the runtime:
#   .venv/Scripts/python -m pip install -c .venv/constraints.txt -r requirements-dev.txt
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27
jiwer>=3.0          # word error rate, eval harness
pyannote.metrics>=3.2  # diarization and Jaccard error rates, eval harness
ruff>=0.6
